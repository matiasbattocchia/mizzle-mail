{
  "name": "mizzle",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Email as an ephemeral feed — your Gmail inbox as a stream that decays. The inbox is a pipe, not a tank. See MANIFESTO.md.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "imapflow": "^1.0.164",
    "nodemailer": "^8.0.10"
  }
}
